#include<bits/stdc++.h>
using namespace std;

int sum(int x,int y)
{
	return (x+y);
}

int32_t main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int x,y;
	cin >> x >> y;
	cout<<sum(x,y)<<endl;
	return 0;
}