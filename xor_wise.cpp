#include<bits/stdc++.h>
#define ll long long
#define sajib ios_base::sync_with_stdio(0); cin.tie(0);
using namespace std;
int32_t main(){
	     sajib
	        int t; cin >> t;
	        int ans = 0;
	        while(t--){
	        	 int mn = INT_MAX;
	        	int n,m; cin >> n >> m;
	        	//for(int i=1;i<=min(n,m);i++){
	        		ans = (m^n);
	        		//mn = min(ans,mn);

	        	//}
	        	cout << ans << endl;
	        }
	return 0;
}