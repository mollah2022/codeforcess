#include<bits/stdc++.h>
using namespace std;

int32_t main()
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	 int x;
	 cin >> x;
	  if(x==1)
	  {
	  	cout<<9<<" "<<8<<endl;
	  }
	  else
	  {
	  	cout<<(x*4)<<" "<<(x*3)<<endl;
	  }
	return 0;
}